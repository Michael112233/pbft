{
  "name": "delay-prepare-test",
  "seed": 424242,
  "steps": [
    {
      "id": "delay-prepare-200ms",
      "when": {
        "trigger": "time_since_start",
        "start_ms": 3000,
        "duration_ms": 10000
      },
      "what": "delay_messages",
      "message_filter": {
        "types": [
          "MsgPrepareMessage"
        ],
        "senders": "any"
      },
      "params": {
        "delay_ms": 200
      }
    }
  ]
}

